# Copy values into your shell or a local ignored .env file. ETS4 does not read keys from CLI flags.
OPENAI_API_KEY=replace-with-a-server-side-api-key

# Optional non-secret configuration overrides
ETS4_PROVIDER=mock
ETS4_MODEL=gpt-5.6
ETS4_REFEREE_COUNT=4
ETS4_OUTPUT_DIR=runs
