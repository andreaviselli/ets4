cd /path/to/ets4
source .venv/bin/activate

set -a
source .env
set +a

test -n "$OPENAI_API_KEY" && echo "OpenAI key loaded" || echo "OpenAI key missing"


ets4 review https://arxiv.org/abs/2605.23978 \
  --provider openai \
  --model gpt-5.6 \
  --referees 4 \
  --output-dir ./runs
